<Page x:Class="WpfApp1.Pages.CreatePlantPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="800"
      Title="Добавить / Редактировать растение">

    <Grid Background="White">
        <Border Background="#F4F4F4"
                Padding="20"
                Width="400"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Фото -->
                    <Image x:Name="imgCover"
                           Width="120" Height="120"
                           Stretch="UniformToFill"
                           Margin="0,0,0,10"/>

                    <!-- Название -->
                    <Label Content="Название растения:" Foreground="#2C3E50"/>
                    <TextBox x:Name="tbName"
                             Text="{Binding name, Mode=TwoWay}"
                             Width="360" Margin="0,0,0,10"/>

                    <!-- Описание -->
                    <Label Content="Описание:" Foreground="#2C3E50"/>
                    <TextBox x:Name="tbDescription"
                             Text="{Binding description, Mode=TwoWay}"
                             Width="360" Height="100"
                             TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,10"/>

                    <!-- Категория -->
                    <Label Content="Категория:" Foreground="#2C3E50"/>
                    <ComboBox x:Name="cbCategories"
                              Width="360"
                              DisplayMemberPath="name"
                              SelectedValuePath="id"
                              SelectedValue="{Binding category_id, Mode=TwoWay}"
                              Margin="0,0,0,10"/>

                    <!-- Цена -->
                    <Label Content="Цена (₽):" Foreground="#2C3E50"/>
                    <!-- Без StringFormat, чтобы не добавлялись разделители -->
                    <TextBox x:Name="tbPrice"
                             Text="{Binding price, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                             Width="360" Margin="0,0,0,10"/>

                    <!-- Кнопки -->
                    <Button x:Name="bImage"
                            Content="Загрузить изображение"
                            Background="#8EB7A7"
                            Click="bImage_Click"
                            Width="300" Margin="0,10,0,0"/>

                    <Button x:Name="bSave"
                            Content="Сохранить растение"
                            Background="#4CAF50" Foreground="White"
                            FontWeight="Bold"
                            Width="360" Margin="0,10,0,0"
                            Click="bSave_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
